# Optional — baked in at build time for production client fetches:
# VITE_API_BASE_URL=https://shelftxt.onrender.com

# Show the shared-library demo banner (public demo only):
# VITE_DEMO_MODE=true

# Self-hosted / private deployments — omit or set explicitly:
# VITE_DEMO_MODE=false
